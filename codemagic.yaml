workflows:
  android-workflow:
    name: Android Build

    environment:
      flutter: stable

    scripts:
      - flutter clean
      - flutter pub get
      - flutter build apk --release
      - mkdir -p artifacts
      - find . -name "*.apk" -type f -exec cp {} artifacts/ \;
      - ls -R artifacts

    artifacts:
      - artifacts/*.apk
